version: '3.8'

services:
  postgres_sn:
    container_name: postgres_sn
    image: postgres
    ports:
      - 5434:5432
    environment:
      - POSTGRES_USER=admin
      - POSTGRES_PASSWORD=admin
      - POSTGRES_DB=pg_sn